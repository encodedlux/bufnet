const net_client = require("../net/net_client")

const my_state = net_client.my_state

my_state.listen(function(data)
	print(data)
end)

local doubled_count = my_state.derive(function(data)
	return data.count
end, function(count: number)
	return count * 2
end)

my_state.listen(doubled_count, function(count, prev_count)
	print(count, prev_count)
end)
